#include <stdio.h>

/**
 * main - Entry point of the program numbers of base 10
 *
 * Return: 0 on successful execution
 */

int main(void)
{
	int number = 0;

	while (number < 10)
	{
		printf("%d", number);
		number++;
	}
	printf("\n");
}
