#include "main.h"
/**
 * mul - checks for lowercase character
 * @a: first input number
 * @b: second input number
 * Return: the result multiplies two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
